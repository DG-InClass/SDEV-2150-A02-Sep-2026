# Lesson 2 Homework

```ps
pnpm init
pnpm add -D vite
```

Open `package.json` and change line 7 to be the following:

```diff
-    "test": "echo \"Error: no test specified\" && exit 1"
+    "dev": "vite"
```

## TODO

1. Create an `index.html`
1. Create a `js/main.js`
1. Add a `<script>` to the HTML
1. .... continue on
